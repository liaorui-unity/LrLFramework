using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

namespace Pool.Log
{
    //[CustomEditor(typeof(PoolShowLog))]
    //public class PoolShowLogEditor : Editor
    //{
    //    public override void OnInspectorGUI()
    //    {   
    //        if (PoolShowLog.builderTips.Length > 0)
    //            EditorGUILayout.HelpBox(PoolShowLog.builderTips.ToString(), MessageType.Info);

    //        if (PoolShowLog.builder.Length > 0)
    //            EditorGUILayout.HelpBox(PoolShowLog.builder.ToString(), MessageType.Warning);

    //        EditorGUILayout.Space(20);

    //        if (PoolShowLog.builder.Length <= 0)
    //        {
    //            EditorGUILayout.HelpBox("数据正常！", MessageType.Info);
    //        }
    //    }
    //}
}